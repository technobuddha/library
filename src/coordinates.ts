/**
 * @group Geometry
 * @category Cartesian / Polar Coordinates
 */
export type Cartesian = {
  x: number;
  y: number;
};

/**
 * @group Geometry
 * @category Cartesian / Polar Coordinates
 */
export type Polar = {
  radius: number;
  angle: number;
};
