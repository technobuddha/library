[**@technobuddha/library**](../README.md)

***

[@technobuddha/library](../README.md) / ensurePrefix

# Function: ensurePrefix()

> **ensurePrefix**(`input`, `prefix`): `string`

Defined in: [ensure-prefix.ts:8](https://github.com/technobuddha/library/blob/main/src/ensure-prefix.ts#L8)

Add a prefix to a string, if it does not already have the prefix

## Parameters

### input

`string`

The string

### prefix

`string`

The prefix

## Returns

`string`

The prefix followed by the string
