/**
 * A function that intentionally does nothing.
 *
 * Useful as a default callback or placeholder implementation when a callable is required.
 * @returns Always returns `undefined`.
 * @example
 * ```typescript
 * noop();
 * ```
 * @group Function
 * @category Utilities
 */
export function noop(): void {
  //
}
