import 'mocha';
import { expect }   from 'chai';
import formatNumber from '../formatNumber';

describe(
    'formatNumber',
    () => {
        it(
            'should have some unit tests',
            () => {
                expect(formatNumber).to.equal(null);
            }
        );

    }
);

